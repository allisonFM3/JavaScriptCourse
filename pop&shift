function popShift(arr) {
  let popped = arr.pop(); // last element in arr
  let shifted = arr.shift(); // first element in arr
  return [shifted, popped];
}

console.log(popShift(['challenge', 'is', 'not', 'complete']));
